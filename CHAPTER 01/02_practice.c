#include<stdio.h>

int main(){
    int radius=5;
    float pi=3.14;
printf("Area of circle=%f\n",radius*radius*pi);
    int height=8;
printf("volume of cylinder=%f",radius*radius*pi*height);

    return 0;
}