#include<stdio.h>
int main()  {
int n,i,a=0,b=1,sum=0;
printf("Enter a number:");
scanf("%d",&n);
for(i=1;i<=n;i++) {
    if(i==1) { } 
else if(i==2){sum=b;}
        else {
   sum=a+b;
   a=b;
   b=sum;}
       printf("The %d fibonacci number is:%d\n",i,sum); }
   return 0;  }
